{
  "name": "@for/sim",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Simulateur de table headless : joue des scenarios sans navigateur ni appel IA.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:tests": "tsc -p tsconfig.test.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@for/content": "workspace:*",
    "@for/contracts": "workspace:*",
    "@for/db": "workspace:*",
    "@for/engine": "workspace:*",
    "@for/server": "workspace:*",
    "@for/testkit": "workspace:*"
  },
  "devDependencies": {
    "@for/tsconfig": "workspace:*"
  }
}
