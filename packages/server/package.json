{
  "name": "@for/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Fastify : HTTP, OAuth Discord, hub WebSocket, service applicatif de table.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:tests": "tsc -p tsconfig.test.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@fastify/cookie": "^11.1.2",
    "@for/ai": "workspace:*",
    "@for/content": "workspace:*",
    "@for/contracts": "workspace:*",
    "@for/db": "workspace:*",
    "@for/engine": "workspace:*",
    "fastify": "^5.12.5",
    "fastify-plugin": "^5.1.0",
    "fastify-type-provider-zod": "^7.0.0",
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@for/tsconfig": "workspace:*"
  }
}
