{
  "name": "@for/ai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Adaptateurs du port du conteur, contexte, prompts, assertions de style.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:tests": "tsc -p tsconfig.test.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@for/contracts": "workspace:*",
    "@for/content": "workspace:*",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@for/tsconfig": "workspace:*"
  }
}
