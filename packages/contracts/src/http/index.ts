/**
 * HTTP route schemas — EMPTY ON PURPOSE.
 *
 * M0-08 fills this folder (`auth.ts`, `tables.ts`, `characters.ts`,
 * `content.ts`, `health.ts`, 01-architecture.md section 6) and re-exports them
 * from here, without touching `src/index.ts`.
 */

export {};
