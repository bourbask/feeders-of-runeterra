{
  "name": "@for/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Regles du jeu. Pur : ni entree-sortie, ni horloge, ni hasard ambiant.",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:tests": "tsc -p tsconfig.test.json --noEmit",
    "lint": "eslint .",
    "test": "tsc -b && vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@for/tsconfig": "workspace:*"
  }
}
